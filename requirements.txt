# Dependencias de la FASE ACTUAL (preparación + EDA de INDIGO).
#
# Las librerías de fases posteriores se agregan en el commit de la fase que las
# usa por primera vez, no antes:
#   - Fase de modelado:    la librería de la familia YOLO que se instale entonces
#   - Fase de backend:     fastapi, uvicorn, python-multipart
#
# Las versiones se fijarán exactas (pip freeze) cuando el entorno virtual esté
# creado y validado. Por ahora se declara la versión mínima compatible.

numpy>=1.26
opencv-python>=4.9
python-dotenv>=1.0
pandas>=2.2
matplotlib>=3.8
